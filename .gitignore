# ============================================================
# TIEUDAO (Tiêu Dao Lục) — kho git riêng, tách khỏi MONGGIANGHO.
# Version MÃ NGUỒN + tài liệu + ASSET (deploy GitHub Pages).
# ============================================================

# Asset (webp ~54MB + audio ~32MB) ĐÃ commit cùng repo để deploy GitHub Pages
# (trước đây ignore khi còn png ~358MB; webp ép gọn nên đưa lên luôn).
# images/
# audio/

# Bản nháp snapshot code thủ công (lịch sử cũ trước khi có git)
_backup/

# File server/dev tạm (tự sinh khi verify)
_devserver.ps1

# OS / editor junk
Thumbs.db
.DS_Store
*.tmp
desktop.ini
