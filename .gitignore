# ============================================================
# TIEUDAO (Tiêu Dao Lục) — kho git riêng, tách khỏi MONGGIANGHO.
# Version MÃ NGUỒN + tài liệu. Asset nặng + bản nháp giữ LOCAL (không vào git).
# ============================================================

# Tài nguyên nặng (nhị phân, ~358MB) — giữ local trên ổ, không version qua git
images/
audio/

# Bản nháp snapshot code thủ công (lịch sử cũ trước khi có git)
_backup/

# File server/dev tạm (tự sinh khi verify)
_devserver.ps1

# OS / editor junk
Thumbs.db
.DS_Store
*.tmp
desktop.ini
